# References for `MyTest` alias at usage site
#
# Alias.sol line 8:     MyTest public myTest;
#                        ^cursor (col 4)
#
# Expected: MyTest at 3:16 + MyTest at 8:4  (2 locations, Alias.sol only)
#
#   lsp-bench -c benchmarks/alias-refs-mytest-usage.yaml

project: example
file: Alias.sol
line: 8
col: 4
output: benchmarks/alias-refs-mytest-usage
report: README.md

servers:
  - mmsaki@latest

initializeSettings:
  projectIndex:
    fullProjectScan: true

benchmarks:
  - textDocument/references

methods:
  textDocument/references:
    waitForProgressToken: "solidity/projectIndexFull"
