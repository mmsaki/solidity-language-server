# Emacs

For the full settings schema, see [Setup Overview](/setup).

## Required package

- [lsp-mode](https://github.com/emacs-lsp/lsp-mode) — Language Server Protocol client for Emacs

## LSP configuration

Add the following to `init.el`:

```elisp [init.el]
// [!include ~/snippets/setup/emacs.el]
```

## Lint options reference

Lint options (severity, rule IDs, `only`, `exclude`) are documented in [Setup Overview → Lint values](/setup#lint-values).
